# skills

Hermes custom skills managed by this repository.

Server-side runtime sync:

```bash
make skills-sync
```

Main-PC / WSL runtime sync is handled by `scripts/hcoder`.

Keep reusable procedures here. Put project background and stable notes under `knowledge/`.
